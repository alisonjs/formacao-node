<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Gera Pergunta</title>
</head>

<body>
  <h1>Bem vindo ao Gera Pergunta!</h1>
  <hr>
  <h3>Lista de Perguntas</h3>
  <%= name %>
  <%= lang %>
  <%= company %>
  <%= subscribers %>

  <% if(msg){ %>
    <h3> Erro encontrado </h3>
  <% } else {%> 
    <h3> Nenhum erro encontrado </h3>
  <% } %>

  <% products.forEach(function(product){ %> 
    <hr>
    <h3><%= product.name %></h3>
    <h4><%= product.price %></h4>
  <% }) %> 
</body>

</html>